/**
 * Bucles de ambiente bajo `public/sounds/` (sin extensión; ver `soundAssetUrl`).
 * Cada lista elige una pista al azar al entrar en la pantalla; se reproduce en loop.
 */
export const shopping_loops = ['loops/SLP_Glitch_Beat_BPM100_18'] as const;

export const battle_loops = ['loops/SLP_Glitch_Beat_BPM100_18'] as const;
